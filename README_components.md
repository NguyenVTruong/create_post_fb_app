```
src/
└── components/
├── ui/            # thành phần UI cơ bản
│    ├── Button.tsx
│    ├── Input.tsx
│    ├── Avatar.tsx
│    └── Text.tsx
│
├── feedback/      # thông báo, loading
│    ├── Toast.tsx
│    ├── Snackbar.tsx
│    └── Loading.tsx
│
├── form/
│    ├── Checkbox.tsx
│    ├── Radio.tsx
│    └── FormField.tsx
│
└── layout/
├── Card.tsx
├── ListItem.tsx
└── EmptyState.tsx
```   
### Lib nội bộ
UI component base (Button, Input, Card, Dialog, Avatar…).

Layout wrapper (Screen, Container).

Feedback (Toast, Loading, EmptyState).

Form helper (FormField, DatePicker, OTPInput).

Utility (hooks, context, service).